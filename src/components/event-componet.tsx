import { Message } from "../lib/core"

type EventProps = {
  message: Message
}

const EventComponent = (props: EventProps) => {
  return (
    <>
      EventComponent
    </>
  )
}

export default EventComponent
